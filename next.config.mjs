/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [ "cdn.sanity.io" ], // Add imgur.com to the list of allowed domains

    },
    
  };
  
  export default nextConfig;
  
